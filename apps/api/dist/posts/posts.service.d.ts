import { Repository, DataSource } from 'typeorm';
import { Post } from '../entities/post.entity';
import { CreatePostDto } from './dto/create-post.dto';
import { ExternalSource } from '../entities/external-source.entity';
import { User } from '../entities/user.entity';
import { Mention } from '../entities/mention.entity';
import { Neo4jService } from '../database/neo4j.service';
import { LanguageDetectionService } from '../shared/language-detection.service';
import { MeilisearchService } from '../search/meilisearch.service';
import { NotificationHelperService } from '../shared/notification-helper.service';
import { SafetyService } from '../safety/safety.service';
export declare class PostsService {
    private postRepo;
    private externalSourceRepo;
    private mentionRepo;
    private userRepo;
    private dataSource;
    private neo4jService;
    private languageDetection;
    private meilisearch;
    private notificationHelper;
    private safetyService;
    constructor(postRepo: Repository<Post>, externalSourceRepo: Repository<ExternalSource>, mentionRepo: Repository<Mention>, userRepo: Repository<User>, dataSource: DataSource, neo4jService: Neo4jService, languageDetection: LanguageDetectionService, meilisearch: MeilisearchService, notificationHelper: NotificationHelperService, safetyService: SafetyService);
    create(userId: string, dto: CreatePostDto): Promise<Post>;
    private slugify;
    private isValidUUID;
    findOne(id: string, viewerId?: string): Promise<Post | null>;
    softDelete(userId: string, postId: string): Promise<void>;
    createQuote(userId: string, quotedPostId: string, commentary: string): Promise<Post>;
    getSources(postId: string): unknown;
    getReferencedBy(postId: string): unknown;
}
