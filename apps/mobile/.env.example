# API base URL (no trailing slash). Required for all environments.
#
# Local development – API via nginx (recommended)
# Run the stack with Docker (infra/docker); nginx proxies /api/* to the API.
# Base URL must include /api so nginx forwards to the API (otherwise /explore/newest etc. hit the web app and return 404).
# Use your machine's LAN IP so the device can reach nginx.
# Example: EXPO_PUBLIC_API_BASE_URL=http://192.168.68.112/api
# For simulator on same machine: EXPO_PUBLIC_API_BASE_URL=http://localhost/api
#
# Production – use your API’s HTTPS URL (e.g. https://api.yourdomain.com)
# EXPO_PUBLIC_API_BASE_URL=https://api.yourdomain.com
#
EXPO_PUBLIC_API_BASE_URL=http://localhost/api
