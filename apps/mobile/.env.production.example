# Production Environment Variables for Mobile App
# Copy this to .env.production or set in your build system (EAS, etc.)

# API Configuration (MUST use HTTPS in production)
EXPO_PUBLIC_API_BASE_URL=https://citewalk.com

# Note: Never commit actual production credentials to version control
# Use your CI/CD system or secure secret management
