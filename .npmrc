# Use isolated node linker - each workspace gets completely separate node_modules
# No symlinks, each app has its own full copy of dependencies
node-linker=isolated

# Disable hoisting to ensure complete isolation
shamefully-hoist=false
