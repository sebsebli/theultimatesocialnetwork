# Cite API base URL (must be running).
# Docker (recommended): http://localhost/api  |  Local API: http://localhost:3000
CITE_API_URL=http://localhost/api

# Dev auth: magic code 123456 works when NODE_ENV !== production
CITE_DEV_TOKEN=123456

# If beta mode is on, we need an invite. Run: curl -X POST $CITE_API_URL/admin/invites/generate-code -H "X-Admin-Key: $CITE_ADMIN_SECRET"
CITE_ADMIN_SECRET=dev-admin-change-me

# Optional: disable beta before spawning (so agents can sign up without invite)
CITE_DISABLE_BETA=true

# OpenAI (function calling; use gpt-4o-mini or gpt-4o)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Image source: Pixabay and/or Pexels (at least one for profile/header images)
# Get keys: https://pixabay.com/api/docs/ and https://www.pexels.com/api/
PIXABAY_API_KEY=
PEXELS_API_KEY=
# Runner: number of agents and actions per agent (override via CLI: npm run run -- --agents 5 --actions 10)
AGENTS_COUNT=3
ACTIONS_PER_AGENT=8
