#cloud-config
package_update: true
package_upgrade: true
packages:
  - docker.io
  - docker-compose
  - git

runcmd:
  - systemctl enable docker
  - systemctl start docker
  - mkdir -p /opt/Citewalk
  # In a real setup, you'd pull from a registry or git repo here
  # - git clone ... /opt/Citewalk
  # - cd /opt/Citewalk && docker-compose up -d
