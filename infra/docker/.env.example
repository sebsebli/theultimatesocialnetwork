# Database Configuration
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Neo4j Configuration
NEO4J_PASSWORD=password

# Meilisearch Configuration
MEILI_MASTER_KEY=masterKey

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Redis Configuration (Required for Queue/Cache)
REDIS_URL=redis://redis:6379

# AI Configuration (Required for Moderation)
OLLAMA_HOST=http://ollama:11434

# JWT / Auth: used for signing and verifying tokens
JWT_SECRET=your-secret-key-change-in-production

# Node Environment
NODE_ENV=production

# API Configuration
FRONTEND_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3000

# Admin: generate invite codes without login (X-Admin-Key header)
# Example: curl -X POST http://localhost/api/admin/invites/generate-code -H "X-Admin-Key: YOUR_SECRET"
CITE_ADMIN_SECRET=dev-admin-change-me

# SMTP Email Configuration
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Cite System <sebastian@lindner.me>"

# Email legal footer (links use FRONTEND_URL; required for compliance)
# FRONTEND_URL is used for Website / Privacy / Terms links in emails
# EMAIL_COMPANY_NAME=Cite
# EMAIL_COMPANY_ADDRESS=Your Company, Street, City, Country